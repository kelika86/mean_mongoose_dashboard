<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>Page Title</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" type="text/css" media="screen" href="main.css" />
        <script src="main.js"></script>
    </head>
    <body>
        <h1>My Mongooses</h1>
        <a href='/mongooses/new'>Add the Mongoose</a> <!--add url-->
        <% for (var mongoose of allMongooses) { %>
            <a href="/mongooses/<%= mongoose.id %>"><%= mongoose.name %></a> <!--print x first to see what data is rendered-->
            <form action='/mongooses/destroy/<%= mongoose.id %>' method='POST'>
                <input type="submit" value="Delete">
            </form>
            <a href='/mongooses/edit/<%= mongoose.id %>'><button>Edit</button></a>
        <% } %>
    </body>
</html>